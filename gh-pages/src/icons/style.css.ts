import { style } from "@vanilla-extract/css";

export const lineStyle = style({
  stroke: "#ffffff",
  strokeWidth: "2px",
  strokeLinecap: "round",
  fill: "none",
});

export const fillStyle = style({
  fill: "#ffffff",
});
